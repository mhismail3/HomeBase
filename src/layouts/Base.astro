---
import '../styles/base.css';
const { title = 'Home Base', description = 'A minimal, content-first hub' } = Astro.props;
const base = Astro.site ? new URL('.', Astro.site).pathname : '/';
---
<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="description" content={description} />
    <title>{title}</title>
    <link rel="canonical" href={new URL(Astro.url.pathname, Astro.site)} />
  </head>
  <body>
    <a class="sr-only" href="#content">Skip to content</a>
    <header class="container">
      <div>
        <strong>boz.</strong>
      </div>
      <nav aria-label="Primary">
        <a href={base}>index</a>
        <a href={`${base}about/`}>about</a>
        <span aria-disabled="true" tabindex="-1">search</span>
      </nav>
    </header>
    <main id="content" class="container">
      <slot />
    </main>
    <footer class="container">
      <small>&copy; {new Date().getFullYear()} — Home Base</small>
    </footer>
  </body>
  </html>
