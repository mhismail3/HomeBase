---
import '../styles/base.css';
const { title = 'Home Base', description = 'A minimal, content-first hub' } = Astro.props;
// Use Astro/Vite base for GitHub Pages subpath. In dev this will be '/'.
const base: string = import.meta.env.BASE_URL || '/';
---
<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="description" content={description} />
    <title>{title}</title>
  </head>
  <body>
    <a class="sr-only" href="#content">Skip to content</a>
    <header class="site-header container">
      <div class="brand">
        <a class="brand-link" href={base}>mohsin ismail</a>
      </div>
      <nav class="site-nav" aria-label="Primary">
        <a href={base}>index</a>
        <a href={`${base}about/`}>about</a>
      </nav>
    </header>
    <main id="content" class="container main">
      <slot />
    </main>
    <footer class="container site-footer">
      <small>&copy; {new Date().getFullYear()} — mohsin ismail</small>
    </footer>
  </body>
  </html>
